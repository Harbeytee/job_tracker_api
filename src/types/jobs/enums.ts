export enum JobStatus {
  Applied = "applied",
  Interview = "interview",
  Accepted = "accepted",
  Rejected = "rejected",
  Ghosted = "ghosted",
}

export enum JobType {
  FullTime = "full-time",
  PartTime = "part-time",
  Remote = "remote",
  Internship = "internship",
}
